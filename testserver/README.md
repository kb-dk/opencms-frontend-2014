Test server
===========
Simple NodeJS webserver to test the ajax enabled menu system locally.
Written in nodeJS.

Prerequisites
-------------
* NodeJS
* npm

Instructions
------------
* npm install
* npm start

Notes
-----
The webroot is set to the parent directory, so the server has to be started from this directory.
If port 8001 is in use, set port with parameter --port=NNNN

